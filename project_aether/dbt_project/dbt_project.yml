name: 'AlphaVantageDataTransformation'
version: '1.0.0'
config-version: 2

profile: 'AlphaVantageDataTransformation'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  +transient: false
  alpha_vantage:
    staging:
      +materialized: view
      +schema: ALPHA_VANTAGE
    intermediate:
      +materialized: view
      +schema: ALPHA_VANTAGE_INTERMEDIATE
#    marts:
#      +materialized: table
#      database: ALPHA_VANTAGE_MARTS
#      +schema: ALPHA_VANTAGE